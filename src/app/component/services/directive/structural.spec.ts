import { Structural } from './structural';

describe('Structural', () => {
  it('should create an instance', () => {
    const directive = new Structural();
    expect(directive).toBeTruthy();
  });
});
