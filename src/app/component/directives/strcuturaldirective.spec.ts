import { Strcuturaldirective } from './strcuturaldirective';

describe('Strcuturaldirective', () => {
  it('should create an instance', () => {
    const directive = new Strcuturaldirective();
    expect(directive).toBeTruthy();
  });
});
